#@ load("@ytt:data", "data")
#@ load("@ytt:assert", "assert")

#@ if not data.values.options["docker-username"]:
#@   assert.fail("--docker-username option required")
#@ end

#@ if not data.values.options["docker-password"]:
#@   assert.fail("--docker-password option required")
#@ end
