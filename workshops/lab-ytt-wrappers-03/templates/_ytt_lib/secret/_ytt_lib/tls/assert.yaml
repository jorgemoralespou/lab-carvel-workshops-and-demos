#@ load("@ytt:data", "data")
#@ load("@ytt:assert", "assert")

#@ if not data.values.options.cert:
#@   assert.fail("--cert option required")
#@ end

#@ if not data.values.options.key:
#@   assert.fail("--key option required")
#@ end
