#@ load("@ytt:data", "data")
#@ load("@ytt:library", "library")
#@ load("@ytt:template", "template")

--- #@ template.replace(library.get(data.values.resource.type).with_data_values(data.values, plain=True).eval())
